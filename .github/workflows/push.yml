on: [push]

jobs:
  testjob:
    runs-on: ubuntu-latest
    name: Test job
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Release
        uses: tudorrrrrr/releases-workflow@v0.0.1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
